[tools]
python = "3"
usage = "latest"
zig = "0.14.1"

[tasks.clean]
description = "Clean build artifacts"
run = "rm -rf zig-out .zig-cache"

[tasks.test]
description = "Run tests"
run = "zig build test"
